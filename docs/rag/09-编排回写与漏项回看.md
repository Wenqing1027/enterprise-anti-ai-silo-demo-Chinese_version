# RAG · R7 编排回写与全链路漏项回看

> 版本：V1.0 · 2026-08-05  
> 触发：完成 `department_flows` RAG 回写后的完整性检查

---

## 1. R7 已回写

| flow_id | demo_ready | skill | 关联 FEATURES |
|---------|------------|-------|---------------|
| `service_repair_qa` | ✅ | `repair_kb` | F-SVC-002 / F-SVC-004 |
| `user_ops_app_qa` | ✅ | `repair_kb` | F-UO-009 |
| `order_policy_qa` | ✅ | `policy_kb` | F-POL-RAG |
| `hr_policy_qa` | ✅ | `hr_rules` | F-HR-001 |
| `channel_ops_board` | ❌（半就绪） | ReAct `channel_ops` ∥ RAG `policy_kb` | 汇合仍 planned |

同步：`docs/planning/02` · `docs/rag/02` · catalog `flow_ids` · README · `agent-orchestration.md`。

---

## 2. 一期 RAG 清单回看（R0–R8）

| 步骤 | 状态 | 说明 |
|------|------|------|
| R0 规划文档三件套 | ✅ | `docs/rag/01–03` |
| R1 切块 | ✅ | `chunks.json` |
| R2 索引 / `search_kb` | ✅ | `tfidf_index.json` |
| R3 Skill 契约 | ✅ | 三 YAML + loader |
| R4 控制环 | ✅ | CLI / `POST /v1/rag/runs` |
| R5 金标 + smoke | ✅ | 15 条 PASS |
| R6 Catalog / UI | ✅ | Demo 卡 + 业务墙/运维分流 |
| R7 编排契约 | ✅ | 本步 |
| R8 旁路 `write_ai_output` | ⏸ | **有意未做**（非 Story1/2 硬依赖） |

---

## 3. 已知缺口（非遗漏事故，属范围外/二期）

| 项 | 判定 |
|----|------|
| `channel_kb` / `product_kb` Skill | 未建；渠道流复用 `policy_kb` |
| 渠道看板自动汇合 | Planning 节点仍 placeholder |
| RAG → AIOutput 资产化 | R8 可选，未实现 |
| 知识库自动更新管线 | Extraction→入库，属二期 |
| embedding / 重排 | 一期坚持 TF-IDF |
| `parallel_orthogonal` 等 orch 文案在业务墙 | 已有 label 映射部分；缺省显示空字符串可接受 |
| recordings 讲稿未写 RAG 段 | 录屏脚本可后续补 30–60s |

---

## 4. 验收命令

```bash
python3 -c "
from apps.catalog import list_flows, get_flows_by_department
fs = list_flows()
need = {'service_repair_qa','user_ops_app_qa','order_policy_qa','hr_policy_qa'}
ids = {f['flow_id'] for f in fs}
assert need <= ids
assert any(f['flow_id']=='service_repair_qa' and f['demo_ready'] for f in fs)
ch = [f for f in fs if f['flow_id']=='channel_ops_board'][0]
assert any(n.get('skill_id')=='policy_kb' for n in ch['nodes'])
print('flows', len(fs), 'rag_demo', sorted(need))
"
```
